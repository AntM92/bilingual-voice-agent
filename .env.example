# Shared secret that ElevenLabs sends in the X-Workspace-Token header on every tool call.
# Set the same value in the dashboard tool config (Headers -> secret).
WORKSPACE_TOKEN=change-me

# Optional: HMAC secret from the ElevenLabs post-call webhook settings page.
POSTCALL_WEBHOOK_SECRET=

PORT=8000
